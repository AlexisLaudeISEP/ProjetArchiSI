librairies utilisées:
  - requests (pip install requests)
  - beautifulSoup4 (pip install beautifulSoup4)
  - json
  - mysql-connector
commandes git :

- git clone adresse ( pour copier le projet)
- git pull
- git add (pour add des fichiers que tu viens de créer )
- git commit -m 'commentaire' (pour ajouter dans l'index)
- git push pour les partager a tout le monde

assurez vous de pull avant de push 
